## Tasogare 黄昏 自选延迟 GPU v2.2.3

基于 Tasogare v1.2.2 整合并增强，版本继续保持 v2.2.3。

### 本次更新

- 节点卡片右上角显示“在线 + 完整天数”
- 例如“在线 10天”，不再显示小时和时钟图标
- 服务器费用移动到在线状态下方
- 直接读取 Komari 节点的价格、币种与计费周期
- 不同节点按后台账单分别显示
- 支持 €17.99/月、€281.7/半年、5/年、免费/月等显示
- 支持常用货币代码转换为货币符号
- 新增半年计费周期显示
- 卡片底部只保留实时上传和下载速度
- 未配置价格的节点自动隐藏费用
- 保留 TCPing 底部安全间距
- 保留窄屏和高 DPI 修复
- 保留 GPU/iGPU、流量周期、访问 IP 等现有功能

### 安装

下载 `komari-theme-tasogare-gpu-2.2.3-komari.zip`，在 Komari 后台主题管理中直接上传 ZIP，不要解压。
